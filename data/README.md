# 📊 Data Description

This directory contains the datasets used in the Titanic Survival Prediction project.

## Structure
- `raw/` – Original dataset files (unaltered)
- `processed/` – Cleaned and preprocessed datasets used for modeling

## Notes
- Raw data is kept unchanged to preserve original information.
- All preprocessing steps are performed via scripts in the `src/` directory.
- Processed data is generated programmatically to ensure reproducibility.
